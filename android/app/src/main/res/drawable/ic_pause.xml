<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <!-- Two dark bars, meaning "tap to pause". Neutral dark gray (matches
         the zoom buttons' icon color, #333333), not red; pause/resume are
         "manage the ongoing recording" actions, not start/stop actions. -->
    <path
        android:fillColor="#333333"
        android:pathData="M7,5h4v14h-4zM13,5h4v14h-4z" />
</vector>
