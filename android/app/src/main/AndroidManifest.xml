<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- MapLibre needs network access to fetch style/tiles; location
         permissions back the Milestone 1 "current location" control.
         COARSE is requested alongside FINE since some OEM location
         dialogs default to offering the coarse grant first. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Milestone 3 (GPS Tracking): a foreground service keeps recording
         going while the phone is locked/backgrounded. FOREGROUND_SERVICE
         plus the location-typed variant (required on Android 14/API 34,
         our targetSdk) are both normal (non-runtime) permissions.
         POST_NOTIFICATIONS (API 33+) is the one dangerous/runtime
         permission here, requested alongside location when the user taps
         "record". -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.KhaoChalakNav">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Milestone 3: foreground service recording the active route. -->
        <service
            android:name=".tracking.TrackingService"
            android:exported="false"
            android:foregroundServiceType="location" />
    </application>
</manifest>
